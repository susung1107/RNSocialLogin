import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    width: '100%',
    height: 40,
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#e5e5e5',
    justifyContent: 'center',
    alignItems: 'center',
  },
  buttonText: {
    fontSize: 14,
    lineHeight: 17,
    color: '#666',
  },
  naverText: {
    color: '#2DB400',
  },
});
